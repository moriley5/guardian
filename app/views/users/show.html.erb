<h2> <%= @user.first_name %> </h2>

<%= link_to "Add Guardian", new_guardian_profile_path %>
<br>
<br>

<div>
  <% @guardians.each do |guardian| %>
    <p>
      <%= link_to guardian.title, guardian_profile_path(guardian) %>
      <br>
      <%= guardian.image %>
    </p>
  <% end %>
</div>
